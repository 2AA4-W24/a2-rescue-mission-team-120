package ca.mcmaster.se2aa4.island.team120;

import org.apache.logging.log4j.LogManager;
import org.apache.logging.log4j.Logger;
import org.json.JSONObject;

public class Navigation{
    //pass different phases 
    //on ground = false find island
    //on ground = true
    //then call simple algo to find creeks on the island
    //if onground is false again but foundground is true, call lost phase
    //class should set a threshold limit 25%
    //if threshold is called then terminate phase
    //if 
}